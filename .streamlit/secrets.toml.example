# Copy to secrets.toml (same folder). That file is gitignored.
# Streamlit Cloud: paste these keys in the app Settings → Secrets editor.

DEFAULT_ADMIN_PASSWORD = "admin123"

# Optional — S3 / R2 backup (see README or s3_sync.py)
# AWS_ACCESS_KEY_ID = "..."
# AWS_SECRET_ACCESS_KEY = "..."
# AWS_REGION = "us-east-1"
# S3_BUCKET = "cuadro-tenis"
# S3_DB_KEY = "tennis/tennis.db"
# R2_ENDPOINT_URL = "https://<ACCOUNT_ID>.r2.cloudflarestorage.com"
# S3_ENDPOINT_URL = "..."  # alternative to R2_ENDPOINT_URL for S3-compatible APIs
